#pragma once
#include <st.h>

#define ST_UTIME_NO_TIMEOUT ((st_utime_t) -1LL)
#define SERVER_LISTEN_BACKLOG 512

namespace st{
	typedef st_netfd_t netfd_t;

	class socket {
	public:

	private:

	};

}